# Binary releases

The binary previously bundled here was built from the legacy v1 source and has been removed from the hardened source archive.

Build a fresh v2 binary locally with:

```bash
make release
```

Do not redistribute a binary as v2 unless it was built from this source tree and passed `--test-all` on the target platform.
